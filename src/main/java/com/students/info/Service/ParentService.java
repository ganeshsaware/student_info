package com.students.info.Service;

import com.students.info.payload.ParentDto;

public interface ParentService {

   // ParentDto saveParent(long studentId, ParentDto parentDto);
    ParentDto saveParent(Long studentId, ParentDto parentDto);
}
